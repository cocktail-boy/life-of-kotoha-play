{
  "name": "life-of-kotoha-play",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Free web version of Life of Kotoha — an ambient world clock for Kotoha.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "sync-assets": "bash scripts/sync-assets.sh"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
